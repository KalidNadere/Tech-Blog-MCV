<!DOCTYPE html>
<html>
<head>
  <title>Edit Blog Post</title>
</head>
<body>
  <h1>Edit Blog Post</h1>
  
  <form action="/api/posts/{{post.id}}" method="PUT">
    <label for="title">Title:</label>
    <input type="text" id="title" name="title" value="{{post.title}}" required>
    
    <label for="content">Content:</label>
    <textarea id="content" name="content" rows="4" required>{{post.content}}</textarea>
    
    <button type="submit">Update Post</button>
  </form>
  
  <!-- link to go back to the dashboard or individual post view -->
  <div class="navigation-links">
    <a href="/">Home</a>
    <a href="/dashboard">Dashboard</a>
    <a href="/post/{{post.id}}">Back to Post</a>
  </div>
</body>
</html>
